server {
  listen       80;
  server_name  _;

  root   /usr/share/nginx/html;
  index  index.html;

  # Serve static data under /data
  location /data/ {
    try_files $uri =404;
    add_header Cache-Control "no-cache";
  }

  # SPA fallback
  location / {
    try_files $uri /index.html;
  }
}

